# Лабораторная работа №6: Генераторы в Python

## Условие задачи

### Вариант 1 — Rare

Реализовать генератор случайных чисел в заданном диапазоне. Не использовать готовые реализации ГПСЧ (модуль `random`).

**Требования:**
- Генератор принимает `min_val`, `max_val` и опциональный `seed`
- Возвращает целые числа в пределах `[min_val, max_val]` через `yield`
- Поддерживает воспроизводимость при одинаковом `seed`
- Обрабатывает ошибки: некорректный диапазон, нецелочисленные границы

### Medium

Написать тесты для генератора с помощью `pytest`.

## Описание проделанной работы

### 1. Что такое генератор

Генератор — это функция, использующая `yield` вместо `return`. При каждом вызове `next()` выполнение продолжается с места последнего `yield`, автоматически сохраняя состояние.

Преимущества:
- Автоматическое сохранение состояния между вызовами
- Экономия памяти (ленивая генерация)
- Чистый и читаемый код

### 2. Реализация декоратора `timer_decorator`

Использует `time.perf_counter()` для высокоточного замера. Через `functools.wraps` сохраняет метаданные функции. Применён к batch-функции для демонстрации.

### 3. Реализация генератора `bounded_random`

Использован **линейный конгруэнтный генератор (LCG)** — классический алгоритм ГПСЧ:
### скриншоты
